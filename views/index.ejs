<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notes loader</title>
        <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
</head>
<body>
<div class="main w-full min-h-screen bg-zinc-900">
    <div class="form p-10 text-white">
        <form action="/ctr" method="post">
            <input class="block px-5 py-2  w-full rounded-md outline-none bg-zinc-800" type="text" placeholder="Title goes here" name="title">
            <textarea class="block px-5 mt-2 w-full rounded-md outline-none py-2 resize-none bg-zinc-800" placeholder="Write your task details here" name="details"></textarea>
            <input class="mt-2 bg-blue-600 rounded-md p-2" type="submit" value="Create task">
        </form>
    </div>
    <div class="tasks flex gap-3 p-10">
        <% if (files.length > 0){ %>
            <%files.forEach(function(val){ %>
       <div class="task w-72 px-3 py-4 rounded-md bg-zinc-800"> 
            <h1 class="text-white "><%= val %></h1>
            <div class="w-full flex justify-between mt-3">
                <a class="text-blue-500 inline-block" href="/file/<%= val %>">Read more</a>
                <a class="text-yellow-700 inline-block right-0" href="/edit/<%= val %>">Edit</a>

            </div>
        </div>
                <% }) %>
            <% } else {%>
<h1 class="text-zinc-600 inline-block mt-3 text-2xl ">No tasks are Performed yet..</h1>
           <% } %>
     
        
    </div>
</div>
</body>
</html>